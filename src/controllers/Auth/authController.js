const handleLogin = require('./handleLogin')
const handleLogOut = require('./handleLogout')
const handleSignUp = require('./handleSignUp')
const handleRefresh = require('./handleRefresh')
const handleGoogleLogin = require('./handleGoogleLogin')
const handleVerifyEmail = require('./handleVerifyEmail')

module.exports = {
    handleLogin,
    handleLogOut,
    handleSignUp,
    handleRefresh,
    handleGoogleLogin,
    handleVerifyEmail
}

